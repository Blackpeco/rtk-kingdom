Shared OnGUI readability helpers in `UiTheme` (World HUD, create, party, battle popups / auto / level-up).  
Framed panels, inset HP/SP bars, element chips, and save toasts live here.  
Battle commands and HP/SP cards are still runtime uGUI in `BattleUI`.
